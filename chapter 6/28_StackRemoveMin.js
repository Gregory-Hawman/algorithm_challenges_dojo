// [] Stack: Remove Stack Min
// Remove a Stack’s minimum value, otherwise leaving values in order.
// If duplicate min values are found, remove them all (see below). Use
// only one additional Queue (plus primitive local vars) for storage.
// Bonus: Create removeNewestMin() and removeOldestMin() that
// remove only one min value.