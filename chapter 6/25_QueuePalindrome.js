// [] Queue: Is Palindrome
// Given a Queue, return true if its values are a palindrome (if they are
// same in reverse order), else return false. Restore Queue to original
// state before exiting. For storage, use one additional Stack.