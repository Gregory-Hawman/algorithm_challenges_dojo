// Create a standalone function that accepts an input string, removes
// leading and trailing white space (at beginning and end only),
// capitalizes the first letter of every word, and returns that string. If
// original string contains the word "Mike" anywhere, immediately
// return "stunned silence" instead.