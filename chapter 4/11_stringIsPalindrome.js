// Create a function that returns a boolean whether the string is a strict
// palindrome. For "a x a" or "racecar", return true. Do not ignore
// spaces, punctuation and capitalization: if given "Dud" or "oho!",
// return false.
// Second: now do ignore white space (spaces, tabs, returns),
// capitalization and punctuation.